---
description: 逆向/VMP/协议/加解密：验证优先、小步迭代、文档记忆、Unidbg MCP 读 trace 与内存
alwaysApply: true
---

## Session 启动

- 若存在 `docs/ANALYSIS.md`，**优先阅读**（总进度、模块索引）。
- 若存在 `docs/RE_FINDINGS.md`（或用户指定的 findings 路径），**读取最新内容**后再推断或改代码；新结论写入对应文档。

## 核心原则

1. **验证优先于结论**：优先产出**可运行、可复现的验证代码**（测试、hex 对比、性质检查）与明确 pass/fail 判据；避免仅凭现象断定「就是 AES/某算法」。可给**待验证假设列表**与顺序。好例：「写脚本验证是否 AES-128-CBC」；差例：「这坨是什么算法」。
2. **小步迭代**：每次**一个子问题**、明确通过条件；禁止在用户未确认上一站时一次性还原整条算法/协议链。
3. **人机分工**：人定方向、实验设计、何时算证毕；AI 写脚本、解析日志/hex、跑命令、整理表格，并把**已证实 / 待验证 / 已否定**写进文档。
4. **文档即记忆**：结论、假说、反例、脚本路径写入 `docs/`；推翻旧结论时同步改文档；**以文档为单一事实源**，不在聊天里堆唯一真相。

## 默认工作流

对齐当前子问题（假设 + 成功判据）→ 产出验证代码/命令 + 预期 → 运行并记入 `docs/RE_FINDINGS.md`（或用户指定文件）→ 通过后再开下一子问题。响应时：区分验证 vs 探索；探索则拆成**第一个**可验证子问题；可附一段便于粘贴进文档的 markdown。

## VMP / 字节码 VM（SO）要点

总线：**SO 去混淆 → IDA 批量导出 → 动态框架 + Trace → Opcode → 字节码提取 → 黑盒/白盒 → 文档 → 端到端验证**。静态几乎失效时：**angr 去 CFF + trace 补洞**互补；大函数用**批量导出到本地文件**按需读，避免交互式塞满上下文；**dispatcher** 上建立 VM 寄存器角色，**栈前后差**推断 opcode 须**多组数据交叉验证**；百万行 trace 用脚本做热点/循环/backjump；白盒认 GF/S-box/MixColumns/CRC 等特征与**表碰撞验证**；黑盒用 dword/byte/nibble **差分**与依赖矩阵；端到端须**固定熵源**再与独立实现逐字节比对。文档建议：`docs/ANALYSIS.md`、`docs/RE_FINDINGS.md`、分模块 md、`docs/archive/` 存已证伪假说。

## Unidbg MCP（关键服务）

用户通过 MCP 暴露 **unidbg trace 文件**与**内存区域**读取。分析指令 trace、热点、内存 dump/切片、与运行态对照时，**必须优先经该 MCP 按需查询**；**不要**默认把完整 trace 或大块内存整段贴进对话或一次性全文读入（除非用户明确要求且体量可控）。**调用 MCP 工具前**先读 schema：工作区有 `mcps/<服务器名>/tools/` 则读其中 JSON；否则以 Cursor 内该 MCP 工具说明为准。

## 语言

用户使用简体中文时，说明与文档用简体中文；标识符、算法名、路径保持原文。
